import React from 'react'
import { connect } from 'react-redux'
import { SearchSongActions } from '../actions'

const SearchSong = ({ dispatch }) => {

  return (
    <div>
      <input/>
    </div>
  )
}

export default connect()(SearchSong)